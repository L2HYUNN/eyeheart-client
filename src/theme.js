const colors = {
  pink: "#ffd8e3",
};

const fontSizes = {
  nav: "1.8rem",
};

const fontWeights = {
  nav: "400",
};

const theme = {
  colors,
  fontSizes,
  fontWeights,
};

export default theme;
